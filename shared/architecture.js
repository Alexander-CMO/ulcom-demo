document.querySelectorAll('.site-toggle').forEach(b=>b.addEventListener('click',()=>{const n=b.closest('header').querySelector('.site-nav');const o=n.classList.toggle('open');b.setAttribute('aria-expanded',String(o))}));document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));document.querySelectorAll('[data-kind]').forEach(c=>c.hidden=b.dataset.filter!=='all'&&c.dataset.kind!==b.dataset.filter)}));document.querySelectorAll('.arc-form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();if(!f.reportValidity())return;const d=new FormData(f);const body=[...d].map(([k,v])=>k+': '+v).join('\n');location.href='mailto:info@svyazconnect.ru?subject='+encodeURIComponent('Запрос по проекту ВОЛС')+'&body='+encodeURIComponent(body)}));